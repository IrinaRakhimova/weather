const About: React.FC = () => (
  <div className="page">
    <h1>About This App</h1>
    <p>This app was built with React, TypeScript and uses the Open-Meteo API to display a simple weather forecast.</p>
  </div>
);

export default About;